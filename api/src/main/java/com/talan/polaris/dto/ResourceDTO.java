package com.talan.polaris.dto;
/*
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.talan.polaris.enumerations.AccountStatusEnum;
import com.talan.polaris.enumerations.ProfileTypeEnum;
import lombok.Data;

import java.time.Instant;
import java.time.LocalDate;

@Data
public class ResourceDTO extends UserTeamDTO
         {


    @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
    public ResourceDTO( @JsonProperty("id") Long id,
                        @JsonProperty("createAt") Instant createAt,
                        @JsonProperty("updateAt") Instant updateAt,
                       @JsonProperty("profileType") ProfileTypeEnum profileType,
                       @JsonProperty("email") String email,
                       @JsonProperty("accountStatus") AccountStatusEnum accountStatus,
                       @JsonProperty("firstName") String firstName,
                       @JsonProperty("lastName") String lastName,
                       @JsonProperty("recruitedAt") LocalDate recruitedAt,
                       @JsonProperty("memberOf") TeamDTO memberOf,
                       @JsonProperty("secretWord") String secretWord,
                       @JsonProperty("passedOnboardingProcess") boolean passedOnboardingProcess) {
        super(id, createAt, updateAt,profileType, email, accountStatus, firstName, lastName, recruitedAt, memberOf, secretWord, passedOnboardingProcess);
    }



    @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
    public ResourceDTO() {
    }

}*/
